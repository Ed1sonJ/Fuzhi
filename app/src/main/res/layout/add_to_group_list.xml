<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">
    <RelativeLayout
        android:id="@+id/id_add_to_group_dialog_titleBar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/baseDialog_layout_height"
        android:background="@color/green_1"
        >
        <ImageView
            android:id="@+id/add_to_group_dialog_img"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:scaleType="fitXY"
            android:src="@drawable/add_to_group"
            android:layout_centerVertical="true"
            android:layout_margin="10dp"/>
        <TextView
            android:id="@+id/add_to_group_dialog_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@+id/add_to_group_dialog_img"
            android:text="添加分组"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:textSize="@dimen/baseDialog_title_textSize"/>
    </RelativeLayout>
    <!--<View-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="1dp"-->
        <!--android:background="#272636"/>-->
    <ListView
        android:padding="3dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/add_to_group_dialog_list"
        android:scrollbars="none"
        android:dividerHeight="1px"/>

</LinearLayout>
